[project]
name = "JMBot"
version = "0.1.0"
description = "JMBot"
readme = "README.md"
requires-python = ">=3.9, <4.0"

[tool.nonebot]
adapters = [
    
]
plugins = []
plugin_dirs = ["plugins"]
builtin_plugins = ["echo", "single_session"]
